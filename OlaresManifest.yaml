olaresManifest.version: '0.12.0'
olaresManifest.type: app
apiVersion: 'v3'

workloadReplicas:
  aimqwen3ttsvllm: 1

metadata:
  name: aimqwen3ttsvllm
  icon: https://raw.githubusercontent.com/bayerhazard/aimighty-qwen3ttsvllm/main/icon.png
  description: Qwen3-TTS-12Hz-1.7B via vLLM-Omni — OpenAI-compatible /v1/audio/speech, 2 task modes, 9 voices, 10 languages, 8 GB VRAM, ~5 min max audio
  appid: aimqwen3ttsvllm
  title: AIM Qwen3 TTS 1.7B
  version: '26.08.4'
  categories:
    - Audio

entrances:
  - name: aimqwen3ttsvllm
    port: 8080
    host: aimqwen3ttsvllm
    title: AIM Qwen3 TTS 1.7B
    icon: https://raw.githubusercontent.com/bayerhazard/aimighty-qwen3ttsvllm/main/icon.png
    openMethod: window
    authLevel: internal

spec:
  versionName: '26.08.4'
  fullDescription: |
    **Qwen3-TTS-12Hz-1.7B** — Alibaba's latest open text-to-speech model, served via vLLM-Omni with a full OpenAI-compatible API.

    **Model**
    Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice (Apache-2.0, commercial use allowed). End-to-end discrete multi-codebook LM architecture at 12 Hz codec rate.

    **Inference Engine**
    vLLM-Omni v0.24.0 (stable). OpenAI-compatible /v1/audio/speech + /v1/audio/voices.

    **Key Features**
    - 2 task modes: CustomVoice (preset speakers), VoiceDesign (voice from description)
    - 9 preset speakers + natural-language style instructions (emotion, speed, tone)
    - 10 languages (ZH, EN, JA, KO, DE, FR, RU, PT, ES, IT)
    - Output formats: wav, mp3, flac, pcm, aac, opus
    - Speed control
    - Extreme low-latency streaming (first audio packet after a single character, ~97 ms e2e)
    - Custom dashboard in AIMightys design language (DE/EN toggle)

    **API Example**
    bash
    curl -X POST http://localhost:8000/v1/audio/speech -H "Content-Type: application/json" \
      -d '{"model": "qwen3-tts", "input": "Guten Tag!", "voice": "ryan", "language": "German", "instructions": "Sprich fröhlich"}' \
      -o output.wav

    **Resource Usage**
    GPU: 1× NVIDIA — 8 GB VRAM slice (fp8 KV cache, gpu_memory_utilization 0.60)
    RAM: 8 GB, CPU: 2 cores
    Disk: 20 GB (model cache, HF_HOME)

    **Capacity**
    - Max audio per request: ~5 minutes (max_model_len 4096 @ 12 Hz codec rate)
    - Concurrent users: 2-3 (e.g. short voice commands) — requests beyond queue automatically
  upgradeDescription: |
    v26.08.4: Tuned for a stable 8 GB VRAM slice — max_model_len 4096 (~5 min audio),
    fp8 KV cache, gpu_memory_utilization 0.60, CUDA_DEVICE_MEMORY_LIMIT_0=8192m.
    Removed Base (voice-clone) task — Qwen3-TTS-12Hz-1.7B-CustomVoice ships no speaker
    encoder, voice cloning requires the separate Qwen3-TTS-12Hz-1.7B-Base model.
    Built for Olares 1.12.6.
  developer: Aimighty
  website: https://github.com/bayerhazard/aimighty-qwen3ttsvllm
  sourceCode: https://github.com/bayerhazard/aimighty-qwen3ttsvllm
  requiredCpu: "2"
  limitedCpu: "8"
  requiredMemory: 8Gi
  limitedMemory: 16Gi
  requiredDisk: 20Gi
  limitedDisk: 50Gi
  requiredGpu: 1
  supportArch:
    - amd64

permission:
  appData: true

options:
  apiTimeout: 0
  allowMultipleInstall: false
  dependencies:
    - name: olares
      version: '>=1.12.6-0'
      type: system
